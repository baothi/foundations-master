require 'rails_helper'

RSpec.describe 'upgrades/show', type: :view do
  before(:each) do
    # @upgrade = assign(:upgrade, Upgrade.create!)
  end

  it 'renders attributes in <p>' do
    # render
  end
end
