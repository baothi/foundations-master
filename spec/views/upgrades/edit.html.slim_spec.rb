require 'rails_helper'

RSpec.describe 'upgrades/edit', type: :view do
  before(:each) do
    # @upgrade = assign(:upgrade, Upgrade.create!)
  end

  it 'renders the edit upgrade form' do
    # render

    # assert_select 'form[action=?][method=?]', upgrade_path(@upgrade), 'post' do
    # end
  end
end
