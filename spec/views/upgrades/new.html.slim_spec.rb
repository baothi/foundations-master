require 'rails_helper'

RSpec.describe 'upgrades/new', type: :view do
  before(:each) do
    # assign(:upgrade, Upgrade.new)
  end

  it 'renders new upgrade form' do
    # render

    # assert_select 'form[action=?][method=?]', upgrades_path, 'post' do
    # end
  end
end
